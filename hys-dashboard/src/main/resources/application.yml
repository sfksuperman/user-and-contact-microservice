server:
  port: 9555

spring:
 application:
  name: hystrix-dashboard

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:3939/eureka/

hystrix:
 dashboard:
  proxy-stream-allow-list: "*"